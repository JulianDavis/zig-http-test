# zig-https-test
A static library built from Zig using the Zig standard library co reate an HTTP/S client that can be used in C.

The goal of this project is to create the smallest executable possible.

## Build
`zig build --release=small`
